# AppWall keeps nothing reflective; defaults from proguard-android-optimize.txt are enough.
# Keep line numbers for readable crash reports (they never leave the device anyway).
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
